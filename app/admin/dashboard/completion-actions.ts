'use server'
import { revalidatePath } from 'next/cache'
import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
function clean(value:FormDataEntryValue|null,maxLength=500){return String(value??'').trim().slice(0,maxLength)}async function requireAdmin(){const supabase=await createClient(),{data:claimsData}=await supabase.auth.getClaims(),userId=claimsData?.claims?.sub;if(!userId)redirect('/admin');const{data:profile}=await supabase.from('admin_profiles').select('user_id').eq('user_id',userId).eq('is_active',true).maybeSingle();if(!profile)redirect('/admin');return supabase}
export async function confirmDefenseCompleted(formData:FormData){const groupId=clean(formData.get('groupId'),36),completionNote=clean(formData.get('completionNote'),500);if(!groupId)redirect('/admin/dashboard?error=Invalid%20research%20group.');const supabase=await requireAdmin(),{data,error}=await supabase.rpc('complete_defense_schedule',{p_group_id:groupId,p_completion_note:completionNote||null});if(error){console.error('Defense completion failed:',error.message);redirect('/admin/dashboard?error=Unable%20to%20confirm%20the%20defense%20as%20completed.')}const result=(data??{}) as{ok?:boolean;error?:string};if(!result.ok)redirect(`/admin/dashboard?error=${encodeURIComponent(result.error||'Unable to confirm the defense as completed.')}`);for(const path of ['/','/schedule','/status','/admin/dashboard','/admin/schedule','/admin/history','/admin/groups',`/admin/groups/${groupId}`])revalidatePath(path);redirect('/admin/dashboard?confirmed=1#action-required')}
