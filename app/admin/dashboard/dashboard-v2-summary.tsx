import Link from 'next/link'
export default function Summary({pending,scheduled,actions}:{pending:number;scheduled:number;actions:number}){return <div className="dashboard-stats" style={{gridTemplateColumns:'repeat(auto-fit,minmax(150px,1fr))'}}><Link className="card dashboard-stat-card stat-pending" href="/admin/groups?status=pending"><span>Pending</span><strong>{pending}</strong><small>Needs scheduling →</small></Link><Link className="card dashboard-stat-card stat-scheduled" href="/admin/schedule"><span>Scheduled</span><strong>{scheduled}</strong><small>Open defense schedule →</small></Link><Link className={`card dashboard-stat-card ${actions?'stat-warning':'stat-clear'}`} href={actions?'#action-required':'/admin/schedule'}><span>Action required</span><strong>{actions}</strong><small>{actions?'Confirm or reschedule →':'Nothing waiting'}</small></Link></div>}
