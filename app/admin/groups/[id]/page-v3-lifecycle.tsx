import Link from 'next/link'
import { notFound, redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
import { saveDefenseSchedule } from './actions'
import PanelAssignmentPicker from './panel-assignment-picker'
import WorkspaceFormControls from './workspace-form-controls'
import CopyResearchLink from './copy-research-link'
import StudentAccessKeyControl from './student-access-key-control'

type FacultyRow={id:string;full_name:string;is_active:boolean}
type GroupMemberRow={id:string;full_name:string;sort_order:number}
type PanelAssignmentRow={faculty_id:string;panel_role:'chair'|'member';sort_order:number}
type ScheduleRow={id:string;defense_date:string;start_time:string;end_time:string;venue:string;notes:string|null;is_published:boolean;panel_assignments:PanelAssignmentRow[]|null}
type DefenseRow={id:string;defense_type:'title'|'proposal'|'final'|null;status:string;requested_at:string;completed_at:string|null;defense_schedules:ScheduleRow[]|ScheduleRow|null}
const STAGES=['title','proposal','final'] as const
function one<T>(value:T|T[]|null|undefined):T|null{return!value?null:Array.isArray(value)?value[0]??null:value}
function defenseTypeLabel(value:string|null){return value==='title'?'Title Defense':value==='proposal'?'Proposal Defense':value==='final'?'Final Defense':'Defense type not recorded'}
function scheduleHasEnded(date:string,endTime:string){return new Date(`${date}T${String(endTime).slice(0,8)}+08:00`).getTime()<=Date.now()}
function stageDate(defense:DefenseRow|undefined){const schedule=one(defense?.defense_schedules);if(!schedule)return null;const[y,m,d]=schedule.defense_date.split('-').map(Number);return new Intl.DateTimeFormat('en-PH',{month:'short',day:'numeric',year:'numeric',timeZone:'UTC'}).format(new Date(Date.UTC(y,m-1,d)))}

export default async function ResearchGroupWorkspaceV3({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{error?:string;saved?:string;reschedule?:string}>}){
 const{id}=await params,{error,saved,reschedule}=await searchParams,supabase=await createClient();const{data:claimsData}=await supabase.auth.getClaims(),userId=claimsData?.claims?.sub;if(!userId)redirect('/admin');const{data:adminProfile}=await supabase.from('admin_profiles').select('user_id').eq('user_id',userId).eq('is_active',true).maybeSingle();if(!adminProfile)redirect('/admin')
 const[groupResult,membersResult,defensesResult,facultyResult]=await Promise.all([supabase.from('research_groups').select('id, public_code, title, program, major, research_file_url, contact_person, contact_email, contact_number, instructor_id, adviser_id, status, defense_type, access_key_created_at').eq('id',id).maybeSingle(),supabase.from('group_members').select('id, full_name, sort_order').eq('research_group_id',id).order('sort_order'),supabase.from('research_defenses').select(`id, defense_type, status, requested_at, completed_at, defense_schedules (id, defense_date, start_time, end_time, venue, notes, is_published, panel_assignments (faculty_id, panel_role, sort_order))`).eq('research_group_id',id).order('requested_at',{ascending:false}),supabase.from('faculty').select('id, full_name, is_active').order('full_name')])
 const group=groupResult.data;if(!group)notFound();const members=(membersResult.data??[]) as GroupMemberRow[],defenses=(defensesResult.data??[]) as DefenseRow[],currentDefense=defenses[0]??null,schedule=one(currentDefense?.defense_schedules),faculty=(facultyResult.data??[]) as FacultyRow[],activeFaculty=faculty.filter(p=>p.is_active),facultyNames=new Map(faculty.map(p=>[p.id,p.full_name])),panelAssignments=[...((schedule?.panel_assignments??[]) as PanelAssignmentRow[])].sort((a,b)=>a.sort_order-b.sort_order),chairId=panelAssignments.find(a=>a.panel_role==='chair')?.faculty_id??'',memberIds=panelAssignments.filter(a=>a.panel_role==='member').map(a=>a.faculty_id),instructorName=group.instructor_id?facultyNames.get(group.instructor_id)??'Not found':'Not assigned',adviserName=group.adviser_id?facultyNames.get(group.adviser_id)??'Not found':'Not assigned',programLabel=group.program?`${group.program}${group.major?` - ${group.major}`:''}`:'Not recorded',currentType=currentDefense?.defense_type??null,stageStatus=currentDefense?.status??group.status,hasEnded=Boolean(schedule?.defense_date&&schedule?.end_time&&scheduleHasEnded(schedule.defense_date,schedule.end_time)),needsConfirmation=Boolean(currentDefense?.status==='scheduled'&&hasEnded),schedulable=Boolean(currentDefense&&['pending','scheduled'].includes(currentDefense.status)),scheduleState=schedule?.is_published&&!hasEnded?'Published':hasEnded?'Ended':schedule?'Private':'Not scheduled',totalSchedules=defenses.filter(d=>one(d.defense_schedules)).length,canDeleteRecord=group.status==='pending'&&defenses.length===1&&totalSchedules===0,dateValue=schedule?.defense_date??'',startValue=schedule?.start_time?String(schedule.start_time).slice(0,5):'',endValue=schedule?.end_time?String(schedule.end_time).slice(0,5):''
 const stageMap=new Map<string,DefenseRow>();for(const defense of defenses)if(defense.defense_type)stageMap.set(defense.defense_type,defense)
 return <section className="section workspace-page"><div className="container workspace-layout"><div className="workspace-topbar workspace-refined-topbar"><div className="workspace-breadcrumbs"><Link href="/admin/groups">Research Groups</Link><span aria-hidden="true">/</span><span>{group.public_code}</span></div><Link className="button button-secondary button-small" href="/admin/groups">← Back to Research Groups</Link></div>{saved?<div className="alert alert-success">Schedule saved successfully.</div>:null}{error?<div className="alert alert-error">{error}</div>:null}{needsConfirmation?<div className="alert alert-warning">This defense has ended and is hidden publicly. Confirm completion from the Dashboard, or reschedule it below.</div>:null}{reschedule&&schedulable?<div className="alert alert-warning">Update the date, time, venue, or panel, then save changes.</div>:null}
 <div className="workspace-adaptive-grid"><aside className="workspace-research-column"><div className="card workspace-research-header"><div className="workspace-research-heading"><div className="workspace-research-labels"><span className="code">{group.public_code}</span><span className="defense-type-pill">{defenseTypeLabel(currentType)}</span><span className={`status-pill status-${stageStatus}`}>{stageStatus}</span></div><h1>{group.title}</h1><p>{programLabel} · Adviser: {adviserName}</p></div><div className="workspace-file-actions">{group.research_file_url?<><a className="button workspace-open-file" href={group.research_file_url} rel="noopener noreferrer" target="_blank">Open Research File ↗</a><CopyResearchLink url={group.research_file_url}/></>:<div className="workspace-file-missing">No research file submitted.</div>}{schedulable?<Link className="button button-secondary" href={`/admin/groups/${group.id}/edit`}>Edit Submission</Link>:<small>Research details are protected until the group requests its next defense stage.</small>}</div></div>
 <div className="card info-card workspace-info-card workspace-progress-card"><div className="workspace-card-heading"><div><h3>Defense progress</h3><p>Permanent Research ID across all stages</p></div></div><div className="workspace-stage-progress">{STAGES.map(stage=>{const record=stageMap.get(stage);return <div className="workspace-stage-row" key={stage}><strong>{defenseTypeLabel(stage)}</strong><span className={`status-pill${record?.status==='completed'?' status-completed':record?.status==='scheduled'?' status-published':''}`}>{record?.status??'Not requested'}</span><small>{stageDate(record)??''}</small></div>})}{defenses.some(d=>!d.defense_type)?<div className="workspace-stage-row"><strong>Legacy defense</strong><span className="status-pill">Type not recorded</span><small>Admin correction required before future progression.</small></div>:null}</div></div>
 <div className="workspace-side workspace-info-column"><div className="card info-card workspace-info-card"><div className="workspace-card-heading"><div><h3>Group members</h3><p>{members.length} {members.length===1?'member':'members'}</p></div></div>{members.length?<ol className="member-list workspace-member-list">{members.map(m=><li key={m.id}>{m.full_name}</li>)}</ol>:<p className="muted-text">No group members recorded.</p>}</div><details className="card info-card workspace-info-card disclosure-card"><summary className="disclosure-summary"><span><strong>Submission details</strong><small>Instructor and contact information</small></span></summary><div className="disclosure-body"><dl className="detail-list"><div><dt>Research instructor</dt><dd>{instructorName}</dd></div><div><dt>Contact person</dt><dd>{group.contact_person}</dd></div>{group.contact_email?<div><dt>Email</dt><dd>{group.contact_email}</dd></div>:null}{group.contact_number?<div><dt>Contact number</dt><dd>{group.contact_number}</dd></div>:null}</dl></div></details><div className="card info-card workspace-info-card"><StudentAccessKeyControl groupId={group.id} hasAccessKey={Boolean(group.access_key_created_at)} publicCode={group.public_code}/></div></div></aside>
 {schedulable&&currentDefense?<form action={saveDefenseSchedule} className="card schedule-form workspace-refined-form"><input name="groupId" type="hidden" value={group.id}/><input name="defenseId" type="hidden" value={currentDefense.id}/>{currentType?<input name="defenseType" type="hidden" value={currentType}/>:null}<div className="workspace-form-header"><div><p className="eyebrow">Defense Schedule</p><h2>{schedule?'Update schedule':`Schedule ${defenseTypeLabel(currentType)}`}</h2><p>{schedule?'Change the schedule or panel, then save.':'Set the schedule and assign the panel.'}</p></div><span className={schedule?.is_published&&!hasEnded?'status-pill status-published':'status-pill'}>{scheduleState}</span></div><fieldset className="workspace-form-fieldset"><section className="workspace-form-section"><div className="workspace-section-heading"><div><h3>Schedule</h3><p>Date, time, venue, and defense stage.</p></div></div><div className="field-grid workspace-schedule-fields"><div className="field"><label htmlFor="defenseTypeDisplay">Defense stage</label>{currentType?<input id="defenseTypeDisplay" readOnly value={defenseTypeLabel(currentType)}/>:<select id="defenseTypeDisplay" name="defenseType" required defaultValue=""><option value="">Select legacy defense type</option><option value="title">Title Defense</option><option value="proposal">Proposal Defense</option><option value="final">Final Defense</option></select>}</div><div className="field"><label htmlFor="defenseDate">Date <span className="required-mark">*</span></label><input defaultValue={dateValue} id="defenseDate" name="defenseDate" required type="date"/></div><div className="field"><label htmlFor="startTime">Start time <span className="required-mark">*</span></label><input defaultValue={startValue} id="startTime" name="startTime" required type="time"/></div><div className="field"><label htmlFor="endTime">End time <span className="required-mark">*</span></label><input defaultValue={endValue} id="endTime" name="endTime" required type="time"/></div><div className="field full"><label htmlFor="venue">Venue <span className="required-mark">*</span></label><input defaultValue={schedule?.venue??''} id="venue" maxLength={180} name="venue" required/></div></div></section><section className="workspace-form-section"><div className="workspace-section-heading"><div><h3>Panel</h3><p>Assign the chair and panel members.</p></div></div><PanelAssignmentPicker faculty={activeFaculty.map(({id:facultyId,full_name})=>({id:facultyId,full_name}))} initialChairId={chairId} initialMemberIds={memberIds}/></section><section className="workspace-form-section"><div className="workspace-section-heading"><div><h3>Public visibility</h3><p>Choose whether students can see the schedule details.</p></div></div><label className="workspace-publish-control" htmlFor="isPublished"><input defaultChecked={schedule?.is_published??false} id="isPublished" name="isPublished" type="checkbox"/><span className="workspace-publish-copy"><strong>Show on public schedule</strong><small>Automatically hidden after the defense end time.</small></span></label><details className="workspace-inline-disclosure" open={Boolean(schedule?.notes)}><summary>Administrative notes {schedule?.notes?'· Added':'· Optional'}</summary><div className="workspace-inline-disclosure-body"><div className="field workspace-notes-field"><label htmlFor="notes">Notes <span className="optional-mark">Optional</span></label><textarea defaultValue={schedule?.notes??''} id="notes" maxLength={1000} name="notes"/></div></div></details></section></fieldset><WorkspaceFormControls canDeleteRecord={canDeleteRecord} groupId={group.id} hasSchedule={Boolean(schedule)} locked={false} publicCode={group.public_code} stageStatus={currentDefense.status}/></form>:<div className="card workspace-refined-form workspace-no-active-stage"><p className="eyebrow">Defense Schedule</p><h2>{currentDefense?.defense_type==='final'&&currentDefense.status==='completed'?'Research cycle completed':'Waiting for the next defense request'}</h2><p>{currentDefense?.status==='completed'?'The previous defense is protected. Students can continue this same Research ID from the submission page using their Access Key when they are ready for the next stage.':'There is no schedulable defense stage for this research.'}</p><Link className="button button-secondary button-small" href={`/status?code=${encodeURIComponent(group.public_code)}`}>View Student Status</Link></div>}</div></div></section>
}
